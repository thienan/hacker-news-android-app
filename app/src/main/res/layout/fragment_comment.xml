<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="android.view.View" />

        <import type="android.text.TextUtils" />

        <import type="com.marcelje.hackernews.utils.ItemUtils" />

        <variable
            name="item"
            type="com.marcelje.hackernews.model.Item" />

        <variable
            name="itemUserClickHandlers"
            type="com.marcelje.hackernews.handlers.ItemUserClickHandlers" />

        <variable
            name="activity"
            type="com.marcelje.hackernews.activity.ToolbarActivity" />

        <variable
            name="parent"
            type="String" />

        <variable
            name="poster"
            type="String" />
    </data>

    <ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/view_margin">

            <TextView
                android:id="@+id/tv_comment_info"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/view_margin"
                android:visibility="@{TextUtils.isEmpty(parent) &amp;&amp; TextUtils.isEmpty(poster) ? View.GONE : View.VISIBLE}"
                android:text="@{ItemUtils.getCommentInfo(activity, parent, poster)}"
                android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

            <android.support.v7.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/view_margin"
                android:elevation="@dimen/card_elevation"
                app:cardCornerRadius="@dimen/card_radius">

                <include
                    android:id="@+id/section_comment_main"
                    layout="@layout/item_comment"
                    app:item="@{item}"
                    app:itemUserClickHandlers="@{itemUserClickHandlers}" />

            </android.support.v7.widget.CardView>

            <include
                android:id="@+id/section_comment_list"
                layout="@layout/section_comment_list"
                app:total="@{item.kids.size}" />

        </LinearLayout>
    </ScrollView>
</layout>

